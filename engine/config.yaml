data:
  input_json: monitor_outputs_v2.json

ai:
  model: gemini

# Diretórios para diferentes componentes do sistema
paths:
  models_dir: ai-engine/model-pipeline/models
  output_dir: ../simulator/data/output
  log_dir: ../simulator/logs

logging:
  level: DEBUG
  file: ai_engine.log

api-key:
  google: KEY_VALUE
